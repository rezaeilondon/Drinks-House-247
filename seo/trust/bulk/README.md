# Challenge 21 -> Challenge 25 bulk update

Generated by `seo/trust/challenge25.py` on 2026-09-16 from a fresh snapshot.

The owner decided on 2026-09-16 to adopt **Challenge 25** site-wide, replacing the
older Challenge 21 policy. Challenge 25 is the standard promoted by the Home Office
and the Retail of Alcohol Standards Group and is what most licensing authorities now
expect; Challenge 21 is the weaker, older scheme.

## Already applied live (no import needed)

| Resource | What changed |
|---|---|
| `/pages/challenge-21` -> `/pages/challenge-25` | Handle, title and body rewritten. The "three years above the legal age" reasoning became "seven years", which no regex would have caught. Added the Home Office / RASG attribution. |
| URL redirect | `/pages/challenge-21` -> `/pages/challenge-25`, created and **live**. Unlike the delivery-page redirects, this one fires: the source handle now genuinely 404s. |
| `/pages/terms-and-conditions` | Section B4 rewritten; company number, VAT and premises licence added to B1. |
| `/pages/contact-us` | Policy paragraph and link. |
| `/pages/authenticity-and-sourcing` | Policy section, FAQ prose and FAQPage JSON-LD. |

## In these CSVs

The remaining 58 resources. Columns match the Matrixify format already used in
`seo/internal-links/bulk/`: `ID, Handle, Command, Body HTML`.

## How the transform was verified

- **152 substitutions**, from only four distinct source strings: `Challenge 21`,
  `under 21`, `under the age of 21`, `/pages/challenge-21`.
- **Guards.** A blanket `21 -> 25` would have corrupted whisky age statements
  ("Glenfiddich 21 Year Old", "aged 21"). The transformer asserts that age-statement
  and legal-age patterns are unchanged, and refuses to emit a body if they are not.
- **Legal purchase age preserved exactly**: 71 mentions of "18 or over" / "over 18" /
  "18+" before, 71 after. The legal age is 18 and did not change.
- **All 11 surviving `21`s were checked by hand** and are legitimate: room temperature
  (21 degrees C), cask evaporation (21%), and a publication date of 2026-09-21.

## Re-running

`challenge25.py` is idempotent and asserts on its own output, so it is safe to re-run
against a fresher snapshot if these CSVs go stale before import.
